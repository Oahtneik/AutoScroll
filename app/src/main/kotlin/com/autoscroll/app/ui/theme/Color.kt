package com.autoscroll.app.ui.theme

import androidx.compose.ui.graphics.Color

// ---- Brand palette ---------------------------------------------------------
// Seed colour matches @color/seed in res/values/colors.xml.
val BrandSeed     = Color(0xFF3D5AFE)
val BrandSeedDark = Color(0xFF8C9EFF)

// Surfaces (subtle, low-contrast cards on dark background)
val SurfaceDark         = Color(0xFF121225)
val SurfaceVariantDark  = Color(0xFF1F1F38)
val OnSurfaceDark       = Color(0xFFE6E6F2)

// Light surfaces — kept calm so the bright primary doesn't dominate.
val SurfaceLight        = Color(0xFFFAFAFC)
val SurfaceVariantLight = Color(0xFFE7E8EF)
val OnSurfaceLight      = Color(0xFF1A1A2E)
